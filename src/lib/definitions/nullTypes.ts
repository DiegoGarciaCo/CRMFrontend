export interface NullUUID {
    UUID: string;
    Valid: boolean;
}

export interface NullString {
    String: string;
    Valid: boolean;
}

export interface NullTime {
    Time: string;
    Valid: boolean;
}

export interface NullInt {
    Int32: number;
    Valid: boolean;
}

export interface NullBool {
    Bool: boolean;
    Valid: boolean;
}
